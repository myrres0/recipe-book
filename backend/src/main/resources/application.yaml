spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret_key: ${SECRET_KEY}